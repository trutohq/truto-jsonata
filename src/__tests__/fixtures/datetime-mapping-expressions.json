[
  "$dtFromIso(created_at).toUTC().toISO()",
  "$dtFromIso(created_at).toFormat('yyyy-MM-dd')",
  "$dtFromIso(created_at).toFormat('yyyy-LL-dd')",
  "$dtFromIso(created_at).toMillis()",
  "$dtFromIso(created_at).toSeconds()",
  "$dtFromIso(created_at).toUnixInteger()",
  "$dtFromIso(created_at).toISO()",
  "$dtFromIso(created_at).toISODate()",
  "$dtFromIso(created_at).toISOTime()",
  "$dtFromIso(created_at).toRFC2822()",
  "$dtFromIso(created_at).toHTTP()",
  "$dtFromIso(created_at).toSQL()",
  "$dtFromIso(created_at).year",
  "$dtFromIso(created_at).month",
  "$dtFromIso(created_at).day",
  "$dtFromIso(created_at).hour",
  "$dtFromIso(created_at).weekday",
  "$dtFromIso(created_at).quarter",
  "$dtFromIso(created_at).ordinal",
  "$dtFromIso(created_at).weekNumber",
  "$dtFromIso(created_at).daysInMonth",
  "$dtFromIso(created_at).zoneName",
  "$dtFromIso(created_at).zone",
  "$dtFromIso(created_at).offset",
  "$dtFromIso(created_at).isValid",
  "$dtFromIso(created_at).plus({ 'days': 1 }).toFormat('yyyy-MM-dd')",
  "$dtFromIso(created_at).minus({ 'months': 1 }).toFormat('yyyy-MM')",
  "$dtFromIso(created_at).startOf('day').toISO()",
  "$dtFromIso(created_at).endOf('month').toFormat('yyyy-MM-dd')",
  "$dtFromIso(created_at).setZone('UTC').toFormat('HH:mm')",
  "$dtFromIso(created_at).set({ 'hour': 0 }).toFormat('HH')",
  "$dtFromIso(created_at).valueOf()",
  "($a := $dtFromIso(created_at); $a.equals($a))",
  "($a := $dtFromIso('2024-01-15T12:00:00.000Z'); $b := $dtFromIso('2024-01-15T00:00:00.000Z'); $a.hasSame($b, 'day'))",
  "($a := $dtFromIso('2024-01-15T12:00:00.000Z'); $b := $dtFromIso('2024-01-15T00:00:00.000Z'); $a.diff($b, ['minutes']).toObject().minutes)",
  "($a := $dtFromIso('2024-01-15T12:00:00.000Z'); $b := $dtFromIso('2024-01-15T00:00:00.000Z'); $a.diff($b).as('hours'))",
  "($a := $dtFromIso('2024-01-15T12:00:00.000Z'); $b := $dtFromIso('2024-01-15T00:00:00.000Z'); $a.diff($b).toMillis())",
  "$dtFromFormat(sys_created_on, 'yyyy-MM-dd HH:mm:ss').toISO()",
  "$dtFromFormat(datecreated, 'LL/dd/yyyy').toISODate()",
  "$dtFromFormat('1667313600', 'fromSeconds').toSeconds()",
  "$dtFromFormat('2024-01', 'yyyy-LL').plus({ 'months': 1 }).toFormat('yyyy-LL')"
]
