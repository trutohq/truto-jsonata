[
  "$dtFromFormat('12:30:00', 'hh:mm:ss').diff($dtFromIso('2024-01-15T00:00:00.000Z').startOf('day'), ['minutes']).toObject().minutes",
  "$dtFromFormat('2024-01-15', 'yyyy-MM-dd').endOf('month').toFormat('yyyy-MM-dd')",
  "$dtFromFormat(closed_at, \"yyyy-MM-dd HH:mm:ss\").toUTC()",
  "$dtFromFormat(sys_created_on, 'yyyy-MM-dd HH:mm:ss').toISO()",
  "$dtFromIso($created_at).toSeconds()",
  "$dtFromIso($now()).endOf('month').toISO()",
  "$dtFromIso($now()).startOf('day').toISO()",
  "$dtFromIso(attributes.date).toFormat('yyyy-LL-dd')",
  "$dtFromIso(body.created_at).toISODate()",
  "$dtFromIso(body.due_date).toFormat('yyyy-LL-dd')",
  "$dtFromIso(body.issue_date).toFormat('yyyy-LL-dd')",
  "$dtFromIso(created_at).toMillis()",
  "$dtFromIso(created_at).toUTC().toISO()",
  "$dtFromIso(created_at).toUnixInteger()",
  "$dtFromIso(created_at).weekday",
  "$dtFromIso(created_at).zoneName",
  "$dtFromIso(created_at.gt).toMillis()",
  "$dtFromIso(created_at.gte).toMillis()",
  "$dtFromIso(created_at.lt).toMillis()",
  "$dtFromIso(created_at.lte).toMillis()",
  "$dtFromIso(end_date.gt).toFormat('yyyy-MM-dd')",
  "$dtFromIso(end_date.gte).toFormat('yyyy-MM-dd')",
  "$dtFromIso(end_date.lt).toFormat('yyyy-MM-dd')",
  "$dtFromIso(end_date.lte).toFormat('yyyy-MM-dd')",
  "$dtFromIso(query.completed_at).toFormat(\"yyyy/LL/dd\")",
  "$dtFromIso(query.created_at).toFormat(\"yyyy-LL-dd\")",
  "$dtFromIso(query.created_at.gt).toFormat('yyyy-MM-dd')",
  "$dtFromIso(query.created_at.gt).toISODate()",
  "$dtFromIso(query.created_at.gte).toFormat('yyyy-MM-dd')",
  "$dtFromIso(query.created_at.gte).toISODate()",
  "$dtFromIso(query.created_at.lt).toFormat('yyyy-MM-dd')",
  "$dtFromIso(query.created_at.lt).toISODate()",
  "$dtFromIso(query.created_at.lte).toFormat('yyyy-MM-dd')",
  "$dtFromIso(query.created_at.lte).toISODate()",
  "$dtFromIso(query.end_date).toFormat('yyyy-LL')",
  "$dtFromIso(query.end_date).toISODate()",
  "$dtFromIso(query.start_date).toFormat('yyyy-LL')",
  "$dtFromIso(query.start_date).toISODate()",
  "$dtFromIso(query.updated_at).toFormat(\"yyyy-LL-dd\")",
  "$dtFromIso(response.created_at).toUTC()",
  "$dtFromIso(response.modified_at).toUTC()",
  "$dtFromIso(user.created_on).toUTC()"
]
